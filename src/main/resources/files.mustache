{{#files}}
    <Component Id='{{componentId}}' Guid='{{componentUUID}}' SharedDllRefCount='{{componentShared}}'>
        <File Id='{{fileId}}' Name='{{fileName}}' DiskId='1' Source='{{fileSource}}' KeyPath='yes'>
        </File>
        {{#mainExecutable}}
            {{#info.autostart}}
        <RegistryValue Id="autostartMainExecutable" Root="HKMU" Action="write"
                       Key="Software\Microsoft\Windows\CurrentVersion\Run"
                       Name="{{info.productName}}"
                       Value="[#{{fileId}}]{{info.autostartParameter}}"
                       Type="string" />
            {{/info.autostart}}
        {{/mainExecutable}}
    </Component>
{{/files}}
